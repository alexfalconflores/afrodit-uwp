<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Afrodit.WinUI.Controls">

    <Style TargetType="local:OverlayView">
        <Setter Property="Background" Value="{ThemeResource AcrylicInAppFillColorDefaultBrush}" />
        <Setter Property="Padding" Value="24" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:OverlayView">
                    <Grid
                        x:Name="PART_LayoutRoot"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        TabFocusNavigation="Cycle">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid x:Name="PART_Header" Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ContentPresenter
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Title}"
                                ContentTemplate="{TemplateBinding TitleTemplate}" />

                            <Button
                                x:Name="PART_CloseButton"
                                Grid.Column="1"
                                Padding="8"
                                AutomationProperties.Name="{TemplateBinding CloseButtonToolTip}"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                ToolTipService.ToolTip="{TemplateBinding CloseButtonToolTip}">
                                <FontIcon FontSize="12" Glyph="&#xE8BB;" />
                            </Button>
                        </Grid>

                        <ContentPresenter
                            x:Name="PART_ContentPresenter"
                            Grid.Row="1"
                            Margin="0,12,0,0"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>